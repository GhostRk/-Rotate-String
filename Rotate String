class Solution {
    public boolean rotateString(String s, String goal) {
       
        String check;
        check=goal+goal; //add target string to each other and search for s(source string)
        if(s.length()!=goal.length())
            return false;
        else if(check.contains(s))
        return true;
        
        return false;
        
    }
}
